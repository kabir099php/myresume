# ---- Server ----
PORT=5000

# ---- Contact form email (optional) ----
# Leave blank to just log submissions to the console.
# Example using Gmail / a transactional provider (SendGrid, Mailgun, etc.)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Where contact-form messages are delivered (defaults to SMTP_USER)
CONTACT_TO=
